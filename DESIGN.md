# CONCOST 기술본부 QC 스튜디오 — UI 시스템

## 2026-09-07 적용 우선순위

v16 사용자 지정 색상 역할: 새 프로젝트는 컨코스트 오렌지 `#FF6B00`와 고대비 진한 글자, 저장·검수·다음 단계 실행은 파랑 `#1D4ED8`, 삭제는 빨강 `#C6263F`. 직접 입력 가능한 셀은 연노랑 `#FFF8D6`; 원본 표와 읽기 전용·비활성 칸은 노랑을 쓰지 않는다. 두 AI 검수 선택은 미선택 흰색, 선택만 연오렌지 `#FFF0DE`와 진한 오렌지 테두리·체크·현재 선택 라벨을 쓴다. 검수 선택 제목은 데스크톱 28px, 모바일 최소 22px. 선택의 의미와 업무 구분색을 혼용하지 않는다. 이 지정은 아래 과거 색상 지침보다 우선한다.

현재 실행 스타일은 `app/workstation.css` 한 곳이다. 과거 `globals.css`, `qto-prototype.css`, `claim-center-theme.css`는 더 이상 layout에서 불러오지 않는다. 아래 과거 토큰보다 현재 스타일을 우선한다. 밝은 하늘색 배경 #F0F6FC, 흰 원본 패널, 파랑 #22649E 주 행동, 핑크 #FFF1F7 마감팀, 민트 #EDF9F5 완료 상태. 본문 14px/1.6, 제목26px. 한국어 긴 라벨을 한 글자씩 세로로 꺾지 않는다.

프로젝트 탐색은 좌측, 업무 단계는 상단. 업로드/저장/검수 이동은 긴 자료보다 위에 둔다. 일반 검수 화면은 자료 자동 확인·저장 → 승인된 지침으로 검수 → 원본·근거·사람 판단의 작업실이다. 지침 작성·시험은 관리자 전용 설정으로 분리한다. 산출식/중복 ITEM 선택 시 제목·결과·평가 범위가 함께 바뀌어야 한다. 장식용 통계나 가짜 실행 결과를 채우지 않는다. 튜토리얼은 업무 범위 확정 후 별도 적용한다. 상세 사용/한계는 `docs/CONCOST_QC_WORKSTATION_GUIDE.md`를 따른다.

## 제품 성격

물량산출 완료 후 PM이 장시간 사용하는 구조·마감 QA 운영 콘솔이다. 웹의 넓은 정보 구조, 필터, 비교 매트릭스, 반응형 탐색을 활용하고 데스크톱 EXE의 패널을 그대로 복제하지 않는다. 핵심 문구는 `FIN 자료`가 아니라 **산출서와 집계표**로 통일한다.

## 시각 언어

- 클레임센터 스튜디오의 실제 라이트 테마를 기준으로 `#EEF3F8` 작업면, `#FFFFFF` 카드, `#17202E` 본문을 사용한다.
- 흰색 사이드바와 옅은 청회색 작업면을 명확히 분리한다. 단계색은 자료 등록=Brown Orange, AI 검수=Blue, 분석표=Green의 세 체계만 사용하고 오류 Red는 확정 오류에만 사용한다.
- 300px 고정 좌측 프로젝트 내비게이션, 넓은 분석 작업면, 밀도 높은 표·2열 비교 매트릭스를 조합한다.
- 본문은 Noto Sans KR 계열, 숫자·수식·코드는 DM Mono 계열과 tabular figures를 사용한다.
- Level A/B/C, 오류·주의·완료는 색과 한국어 상태 라벨을 함께 표시

## 정보 구조

1. 좌측 고정 영역: 프로젝트 추가, 검색, 등록 프로젝트 목록, 설정. 선택한 프로젝트가 전체 작업 경계가 된다.
2. 수량산출 분석표 하위: 구조(보·슬라브·기둥·옹벽·기초·아파트슬라브·아파트옹벽)와 마감(내부·외부 면적, 조적·창호 수량)
3. 하단 프로필: 인증 계정·접근 상태. 로컬 우회 모드는 화면 상단에 명시적인 비운영 배너 표시
4. 상단 헤더: ERP 미연동 상태, 프로젝트명 셀렉터, 검수 엔진 상태. 실행 불가 액션은 이유를 텍스트로 공개
5. 상단 3단계 레일: STEP 1 자료 등록 / STEP 2 AI 검수 / STEP 3 수량산출 분석표. STEP 2에서는 산출식과 중복 ITEM을 같은 크기의 큰 선택 카드로 제공한다.
6. 프로젝트 자료 화면은 좌측에서 확정된 프로젝트를 그대로 사용한다. 같은 프로젝트를 오른쪽에서 다시 검색·확정하게 하지 않는다.

모바일에서는 같은 내비게이션을 텍스트 라벨이 유지되는 서랍으로 바꾸며, 프로젝트 표는 핵심 CTA가 화면 밖으로 숨지 않는 카드 목록으로 변환한다.

## 토큰

- 배경 `#EEF3F8`, 카드·내비게이션 `#FFFFFF`, 보조 작업면 `#F8FAFC`
- 자료 등록 `#9B4E29`/`#FFF4ED`, AI 검수 `#28699F`/`#EDF6FD`, 분석표·완료 `#246F5A`/`#EEF8F4`, 오류 `#B63B42`
- 본문 `#17202E`, 보조 `#526277`, 경계 `#D8E2EE`, 강한 경계 `#C3D0DE`
- 간격은 4/8 기반, 주 섹션은 16/24/32/48 계층
- 본문 기본 글꼴: Noto Sans KR → Pretendard → system-ui → sans-serif
- 수식·수량: DM Mono → JetBrains Mono → Consolas → monospace

## 상호작용 원칙

- 화면당 주 CTA 하나, 나머지는 보조·비활성 상태를 명확히 구분
- 네트워크 작업 중 제출 버튼 비활성화 및 진행 문구 제공
- 실패 메시지는 원인과 `다시 시도` 경로를 함께 제공
- 드래그앤드롭이 생기더라도 파일·폴더 선택 버튼을 항상 병행
- 모든 아이콘 버튼에 접근 가능한 이름을 제공하고, 텍스트 옆 장식 아이콘은 접근성 트리에서 제외
- 포커스 링, 건너뛰기 링크, reduced-motion을 기본 지원

## 금지 패턴

- 아이콘만 있는 1차 내비게이션
- 실제 기능이 없는 빈 검은 작업면
- AI가 결정론 결과를 덮어쓰거나 확정하는 표현
- 엔진이 실행되지 않았는데 `0건`, 성공 체크, 실제 후보처럼 보이는 예시를 표시하는 흐름
- 조적을 계산에 포함하거나, 다른 부위를 동일 아이템으로 묶는 흐름
- Sites 인증을 회사 임직원 명부 검증으로 과장하는 상태
- 검수 기능 선택에 작은 탭만 사용하거나 다크 그라데이션 배너를 겹쳐 쓰는 화면
- 좌측 프로젝트 선택 뒤 같은 프로젝트를 오른쪽에서 다시 검색·확정하게 하는 중복 단계

## 자료 등록과 파스텔 보조 카드

- 설정의 정보 카드는 하늘색 `#EEF7FF`, 파란색 `#EAF3FF`, 분홍색 `#FFF1F7`을 사용한다. 본문은 각각 `#17324D`, `#27466C`, `#653651`로 유지하며 다크 테마의 반투명 회색을 섞지 않는다.
- 프로젝트 선택 후에는 `마감팀` 또는 `구조팀`을 선택하면 업로드가 열린다. 같은 팀을 다시 선택해 새 기록을 만들지 않는다.
- 과거에 같은 팀 기록이 여러 개 생긴 경우 삭제하거나 합치지 않고 `이전 자료 기록` 드롭다운으로 보존한다.
- 산출서·집계표 체크리스트는 파일명 기반 제출 안내다. `선택 · 저장 전`, `저장`, `미등록 · 해당 시 보완`을 구분하며 내용 검증 완료로 표시하지 않는다.
- 미등록 자료가 있어도 서버에서 저장 확인된 자료가 있으면 다음 단계로 진행한다. 차단·충돌 자료는 진행 근거로 인정하지 않으며 부족한 근거를 정상 또는 수량 0으로 해석하지 않는다.
- `STEP 2 · AI 검수 시작`의 초록색 안내·버튼 묶음 자체를 업로드 form의 첫 영역, 제목과 파일 선택보다 위에 배치한다. 하단 중복 영역은 제거한다. 좁은 화면에서는 같은 상단 영역 안에서 버튼을 한 행 전체 너비로 표시한다.

## 체크리스트와 좌측 프로젝트 관리 보완 (2026-09-03)

- 진행 상태 목록의 22px 아이콘 열 규칙은 `.selected-project > ol > li`에만 적용한다. 자료 체크리스트에는 상속시키지 않는다. 체크리스트는 자료명·상태를 한 행에 표시하고 설명을 아래에 배치하며, 좁은 화면에서는 상태가 다음 행으로 내려간다.
- 좌측 `새 프로젝트` 바로 아래에 입력창을 연다. 취소하면 현재 작업 화면을 유지하고, 등록 성공 시 새 프로젝트의 팀 선택으로 이동한다.
- 프로젝트 행에는 선택 버튼과 빨간색 `삭제` 버튼을 형제로 둔다. 삭제는 이름 입력 없이 한 번 확인하며 실패 시 목록과 현재 선택을 보존한다. 삭제 중·파일 저장 중에는 중복 요청을 막는다. 서버의 원본·감사 이력 보존 정책은 그대로 유지한다.

## 자료 재업로드 · 기존 자료 교체 (2026-09-03)

- 업로드 상단의 `STEP 2 · AI 검수 시작`은 항상 위치를 표시한다. 저장된 사용 가능 자료가 없거나 저장 중 또는 저장 내역을 확인하지 못한 상태면 비활성화하며 기존 이동 조건을 유지한다.
- `추가 등록`은 기존 자료를 유지한다. `기존 자료 교체`는 현재 프로젝트·팀의 **선택한 자료 기록** 안에서 대상 묶음과 파일을 펼쳐 확인하고, 한 번 확인한 뒤 새 파일을 저장한다.
- 새 파일 전부의 서버 저장을 확인한 뒤 교체한다. 실패·충돌이면 기존 자료를 유지하고, 준비 중 교체 파일은 검수 입력에 섞지 않는다. 재시도하거나 준비 중 묶음을 삭제할 수 있다.
- 교체된 자료는 현재 목록·체크리스트·검수 대상에서 제외하고 `교체된 이전 자료`에 접어서 보관한다. 원본 파일·감사 기록·이전 검수 결과를 실제로 지우지 않는다.
- 파스텔 하늘색 등록 방식 영역, 네이티브 라디오·확인창, 기존 녹색 이동 버튼을 재사용한다. 새 테마나 애니메이션은 추가하지 않는다.

## 중복 아이템 검수 기준 보완 (사용자 지정)

- 기준 자료는 **동별집계표**다. 전체 아이템을 공종에 걸쳐 비교하므로 현재 공종명이 다르다는 이유만으로 후보에서 제외하지 않는다.
- `공종 오배치 의심`과 `중복 아이템 후보`를 별도로 구분한다. 예를 들어 미장 품목이 금속공사에 있으면 해당 분류와 제안 공종을 PM이 확인한다. 명칭 하나만으로 오분류를 확정하지 않는다.
- 중복 후보는 코드·품명·규격·단위·부위·적용범위를 비교한다. 같은 이름이어도 내부/외부, 벽/바닥/천장 등 범위가 다르면 자동 통합하지 않는다. 수량이 같거나 이름이 유사한 것만으로 중복 물량이라고 단정하지 않는다.
- 결과에는 원본 파일·시트·행, 동·공종, 원본 코드·품명·규격·단위·수량, 의심 이유와 표준화 제안을 연결해야 한다. 원본은 보존하며 PM 승인 전에 이동·병합·수량 변경을 하지 않는다.
- 동별집계표가 없으면 다른 저장 자료로 가능한 검수를 진행하되 전체 아이템 대조 미완료 범위를 명시한다. 이 기준은 UI 안내와 향후 엔진의 수용 기준이며, 현재 파일명 매칭을 실제 AI 검수로 표시하지 않는다.

## Desktop 0.1.1 · 매핑과 관리자 초안 UI (2026-09-09)

이 절은 설치형 시험판 `desktop/ui/`의 현행 화면만 기록한다. 위 웹 UI 지침을 대체하지 않으며 서버 기능·운영 배포 완료를 뜻하지 않는다. 기준은 `index.html`, `app.js`, `instructions.js`, `style.css`이며 미리보기 문자 한도는 `desktop/core.ts`에 따른다.

- 원가관리 참조의 상단 도구 모음과 현재 프로젝트 바를 유지한다. 흰 작업 패널과 밝은 중립 배경 위에 파랑 실행, 연오렌지 선택, 연노랑 입력을 구분한다. 설치형 본문은 맑은 고딕 계열 14px이며 웹의 글꼴·토큰을 교체하지 않는다.
- `열·자료종류 매핑 확인`은 산출서 가져오기 옆에 둔다. 파일·시트 선택 아래 왼쪽 310px 편집 폼과 오른쪽 읽기 전용 원본 표를 배치하고 두 영역을 독립 스크롤한다. 760px 이하에서는 한 열로 전환한다. 원본 표는 실제 행 번호·알파벳 열 이름을 표시하고 머리글과 첫 열을 고정하며 키보드 가로 스크롤 접근을 제공한다.
- 미리보기는 시트별 최대 30행·64열, 셀별 최대 200문자이며 전체 문자 한도로 더 줄어들 수 있다. 원본 전체 표시나 원본 수정 기능으로 표현하지 않는다. 파일·시트 선택 및 매핑 입력은 연노랑, 읽기 전용 원본 셀은 흰 바탕을 사용한다.
- 매핑의 `미확정 · 자동 추정`, `변경됨`, `매핑 확인됨` 상태를 문구로 구분한다. 변경 뒤 재확정 전에는 검수 실행을 막고 `이 시트 매핑 확정`과 `이 시트 변경 취소`를 함께 제공한다. 적용 범위는 이번 PC 작업이며 원본 내용·수량을 변경한다는 표현을 쓰지 않는다.
- `관리자 지침`은 관리자 상태에서만 노출하는 별도 화면이다. 열면 프로젝트·자료등록/일반 검수 결과의 선택 표시와 본문을 해제·숨겨 관리자 화면만 선택되게 하고, 닫으면 이전 작업 탭으로 돌아간다. UI의 메뉴 숨김 자체를 서버 권한 검증으로 설명하지 않는다.
- 관리자 초안은 자료 기록과 복사할 지침 버전을 고른 뒤 이름·작성 이유·항목을 입력한다. 텍스트 입력은 연노랑, `새 초안 버전 저장`은 파랑, `항목 제외`는 빨강을 유지한다. 저장 실패 시 입력 보존과 자동 재시도하지 않음을 안내하며, 미저장 초안을 버리는 재조회·자료 기록/지침 변경에는 확인을 받는다.
- 새 초안 저장과 시험·활성화를 분리해서 안내한다. 초안은 이 PC 일반 기본검사에 아직 적용되지 않으며 서버 등록 자료로 별도 시험·활성화가 필요하다는 제한을 화면 상단에 유지한다. 저장 성공을 검수 적용·승인 완료로 표시하지 않는다.

화면 증거 위치: `output/playwright/desktop-mapping-26b4d843-1d34-4388-adbf-1851b4c4752e/`의 `mapping-1440.png`, `mapping-1000.png`, `instructions-1440.png`, `instructions-1000.png`. 이 문서 기록만으로 기능·접근성·서버 검증 통과를 주장하지 않는다.

## Desktop 0.1.2 · 관리자 지침 시험 UI (2026-09-10)

`desktop/ui/instructions.js`와 `style.css`의 관리자 화면을 기록한다. 기존 웹·설치형 색상과 초안 편집 구조를 유지하며 새 테마나 토큰을 도입하지 않는다.

- 자료 기록과 저장 지침 버전 선택 아래에 `저장된 버전 시험 · 활성화` 영역을 둔다. 저장 버전 선택 → `서버 자료로 시험 · AI 호출 없음` → 평가 범위·한계 확인 → 확인창을 거쳐 활성화하는 순서다. 시험은 파랑 실행 버튼, 조회·활성화는 보조 버튼으로 구분하며 입력 연노랑과 선택 연오렌지를 유지한다.
- 시험 준비 안내는 서버 자료 수와 확인한 매핑 수를 표시한다. 미저장 편집, 서버 자료·확인 매핑 부족 또는 시험·활성화 응답이 불확실한 상태에서는 시험이나 활성화를 진행시키지 않는다. 실패 시 자동 재시도하지 않고 서버 지침 재조회와 시험 이력 확인을 안내한다.
- 시험 결과는 시험 ID·지침 버전·행 수·검토 후보 수와 `검수 규칙 / 평가 / 미평가 / 미평가 사유` 표로 표시한다. 한계는 개수를 표시한 펼침 영역으로, 활성화 차단 사유는 별도 문구로 제공한다. 긴 결과 문구는 줄바꿈하고 행동 버튼은 공간에 따라 다음 줄로 배치한다.
- `미평가는 정상이 아닙니다`를 결과에 명시한다. 활성화는 지침 사용 허용이지 산출량·보고서 최종 승인이 아니며, 활성화 후에도 이 PC의 일반 기본검사에는 적용되지 않는다고 안내한다. 외부 AI 미호출 시험을 AI 지침 검증 완료로 표현하지 않는다.

화면 증거: `output/playwright/desktop-mapping-04a3072f-b0e7-4b63-b282-cee9151bbc81/`의 `instructions-1440.png`, `instructions-1000.png`, `instructions-result-1000.png`. 독립 finish 검토의 ship 판정은 1440px·1000px 및 1000px 펼친 결과의 이번 UI 범위에만 해당한다. 운영 서버·실자료 실증이나 제품 전체 출시 승인을 뜻하지 않는다.
