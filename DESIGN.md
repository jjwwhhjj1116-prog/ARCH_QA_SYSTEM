# FIN & RC Review Studio — UI 시스템

## 제품 성격

장시간 사용하는 건축 물량 검수용 운영 콘솔이다. 장식보다 계보·판정 권한·다음 행동을 먼저 보여준다. 핵심 문구는 `FIN 자료`가 아니라 **산출서와 집계표**로 통일한다.

## 시각 언어

- 고정 그래파이트 좌측 내비게이션 + 밝은 검수 작업면
- CON COST 오렌지는 현재 위치, 주 행동, 포커스에만 사용
- 둥근 카드 남용 없이 선·면·간격으로 데이터 계층을 구분
- 숫자와 코드에는 tabular figures를 사용
- Level A/B/C, 오류·주의·완료는 색과 텍스트를 함께 표시

## 정보 구조

1. 좌측 고정 텍스트 메뉴: 검수 프로젝트 / 자료 라이브러리 / 검수 결과 / 규칙 프로필 / 통계·리포트
2. 상단 상태줄: 제품명, 운영 모드, AI 권한 경계
3. 워크플로 레일: 자료 등록부터 결과 확정까지 7단계
4. 주 작업면: 페이지 제목 → 상태 피드백 → 주 행동 → 데이터 표/빈 상태 → 선택 상세

모바일에서는 같은 내비게이션을 텍스트 라벨이 유지되는 서랍으로 바꾸며, 워크플로는 가로 스크롤 가능한 단계 목록으로 축약한다.

## 토큰

- 배경 `#eef0f3`, 작업면 `#ffffff`, 내비게이션 `#101419`
- 주황 `#c94a0b`(행동), `#ff6a1a`(강조·포커스)
- 본문 `#18202a`, 보조 `#606c79`, 경계 `#c7cdd4`
- 간격은 4/8 기반, 주 섹션은 16/24/32/48 계층
- 본문 기본 글꼴: Pretendard → Noto Sans KR → Malgun Gothic → sans-serif

## 상호작용 원칙

- 화면당 주 CTA 하나, 나머지는 보조·비활성 상태를 명확히 구분
- 네트워크 작업 중 제출 버튼 비활성화 및 진행 문구 제공
- 실패 메시지는 원인과 `다시 시도` 경로를 함께 제공
- 드래그앤드롭이 생기더라도 파일·폴더 선택 버튼을 항상 병행
- 모든 아이콘 버튼에 접근 가능한 이름을 제공하고, 텍스트 옆 장식 아이콘은 접근성 트리에서 제외
- 포커스 링, 건너뛰기 링크, reduced-motion을 기본 지원

## 금지 패턴

- 아이콘만 있는 1차 내비게이션
- 실제 기능이 없는 빈 검은 작업면
- AI가 결정론 결과를 덮어쓰거나 확정하는 표현
- 조적을 계산에 포함하거나, 다른 부위를 동일 아이템으로 묶는 흐름
- Cloudflare 연결 전 원격 저장이 되는 것처럼 보이는 상태
