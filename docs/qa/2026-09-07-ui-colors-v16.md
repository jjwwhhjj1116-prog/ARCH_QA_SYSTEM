# v16 Build Notes — action colors and review selection

2026-09-07. UI-only follow-up to the user's v15 screenshot feedback.

## Scope and acceptance

- UI-001 / UI-005: formula and duplicate review navigation uses large 28px titles
  (24px tablet, 22px mobile), white unselected surfaces and light-orange selected
  surfaces with a visible current-selection label/check and aria-current.
- A11Y-001 / A11Y-005: blue primary execution/save/next-step actions, bright CONCOST
  orange new-project action, red destructive actions. Keyboard focus retained.
- A11Y-002: editable text/number fields and mapping/decision choices are light yellow;
  source tables, read-only and disabled controls are not presented as editable.
- Existing event handlers, save prerequisites, pending-operation locking, review
  semantics, team history, and source lineage are unchanged. No dependencies added.
- No schema, runtime secret, account, permission, audience or storage mutation.
  Employee authentication remains unactivated; v15 limitations still apply.

## Evidence

- `npm.cmd run check`: PASS — lint, formatting, TypeScript, 251 tests in 37 files,
  production build.
- `npm.cmd run test:e2e`: PASS — 10 passed, 6 intentionally skipped. Four widths:
  1440, 1280, 768 and 360. Large workbook test runs only at desktop; drawer interaction
  runs only at mobile. Existing axe checks passed across workflow/settings.
- Added regression assertions: editable field yellow, primary/new-project colors,
  title minimum size, selected/unselected colors, visible selection check, keyboard
  tab/Enter mode navigation, reviewer-note yellow, original-panel preservation,
  and no viewport overflow for the translated results workflow.
- First E2E pass exposed test precondition errors: programmatic focus after a mouse
  click does not request keyboard focus-visible, and a stationary pointer hovered
  a newly inserted mobile card. Tests now use actual Tab and move the pointer away
  for default-state assertions. No production focus rule was weakened.
- Synthetic 24,000-row XLSX, 10,137,513-byte XML: existing baseline persisted;
  measured review time 3,294ms in the final E2E run.
- Main-agent batched visual check: desktop and mobile, including Vietnamese.
  Read-only independent reviewer found no blocking regressions in the source diff
  or generated screenshots. No production workbook was edited for testing.
- Evidence screenshots: `.impeccable/review/colors-*.png`,
  `.impeccable/review/review-vi-*.png`, `.impeccable/review/settings-*.png`.
- Text contrast: orange action 5.74:1, selected mode 7.68:1, editable yellow 12.54:1,
  blue action 6.70:1, red action 5.60:1. Input border on yellow 3.12:1.
- `git diff --check`: PASS. Only scoped UI, test and design-document changes.

## Publishing and rollback

Continue the existing user-authorized private review/deploy workflow at
https://fin-rc-review-studio.yun0421.chatgpt.site/ . Access independently re-read:
owner, custom, exactly one allowed account, no groups, no external visitors.
Do not expand that audience or activate employee login as part of this release.

Rollback target: v15, source `de679b4aa61c1f9ce5003614205153f3158442c2`, saved version
`appgprj_6a9668adf2d48191b92a7f835a6b348f~appgver_3fccf439319481919e4664a71929b237`.
UI-only rollback requires no database migration. Commit, archive, deployment and
live verification are recorded in the release tool output after this candidate.
