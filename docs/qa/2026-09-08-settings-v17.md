# Settings v17

## Delivered

Personal Gemini key entry, model selection, verify-before-save, replacement, confirmed disconnect and reload-persistent encrypted storage. Personal identity is the server-authenticated subject; existing organization administrator allowlist is unchanged. A successful connection test verifies model metadata and generateContent availability, not inference. Basic review remains external-AI independent.

The settings page puts the actionable API form first, retains working language/sidebar preferences, and separates unimplemented ERP/Mem0 integrations from controls. Korean and Vietnamese form copy, pale-yellow editable fields and blue/red actions follow the user's design direction.

## Evidence

- `npm.cmd run check`: lint, formatting, TypeScript, 259 tests in 40 files and build passed before release-marker-only update; final build repeated for the marker.
- `npm.cmd run db:migrate:test`: clean migrations and existing ingestion/idempotency invariants passed.
- `npm.cmd run test:e2e`: 10 passed, 6 existing viewport-specific skips. Four viewports and Vietnamese settings accessibility passed. Local screenshots under ignored `.impeccable/review/settings-*`.
- Synthetic service/API/component tests cover encryption/AAD, persistence, account isolation, failed replacement, stale version, audit, deletion confirmation, plaintext redaction, CSRF, input limits, nonadmin personal ownership and input clearing.
- Independent settings_audit review found no blocker. Its response-size note was fixed with a bounded reader.
- No real user API key was supplied or used. Real provider acceptance still requires the user's key in the deployed form. No paid inference or production source mutation performed.

## Migration / rollback

`0009_personal_ai_settings.sql` adds only two independent tables. Existing 0001–0008 migrations lacked Drizzle metadata: generated current-schema snapshot establishes the new baseline, with only the two new generated CREATE statements retained as 0009. A second `db:generate` reported no changes. Existing migrations are untouched.

`AI_SETTINGS_ENCRYPTION_KEY` is a new independent Sites secret; no value is in source. Do not rotate it while credentials exist. Rollback to v16 (`appgver_7278b609a2288191bd35578444db816c`) leaves additive tables unused and preserves original sources and review history.

Acceptance covered: AUTH-001/005, AUD-001/002, SEC-003, AI-001/002, A11Y-002, REL-002. Live UI smoke and deployment recorded after publish, not claimed by local tests.
