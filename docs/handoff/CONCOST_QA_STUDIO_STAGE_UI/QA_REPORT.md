# CONCOST 기술본부 QC 스튜디오 — 단계형 UI 개편 QA

## 적용 범위

- 공식 CON COST 로고 2종 적용
- 제품 타이틀 `CONCOST 기술본부 QC 스튜디오` 통일
- 프로젝트 등록과 프로젝트 자료 등록 화면 분리
- 좌측 패널과 작업 화면의 명도·색상 대비 분리
- 단계별 강조색 적용: 프로젝트 등록(청록), 프로젝트 자료(파랑), 산출식 AI 검수(주황), 중복 아이템 AI 검수(보라), 분석표(초록)
- 구조·마감 공종별 분석표 하위 메뉴 구성
- 설정 화면에 서버 전용 Gemini 연결 상태와 연결 시험 제공
- 미구현 분석 결과는 `N/A · 미실행`으로 표시

## 검증 결과

- 품질 게이트: lint, format, TypeScript, unit, production build 통과
- 단위·통합 테스트: 106/106 통과
- 브라우저 E2E: 9 통과, 3 의도적 viewport skip
- 접근성: 키보드 탐색, 현재 메뉴, 확장 상태, 모바일 포커스 이동 및 axe 검사 포함
- 보안: Gemini API 키는 브라우저 입력·응답·번들에 포함하지 않고 서버 환경변수만 사용

## 운영 전 설정

- Cloudflare 연결 전 운영 계정은 기존 `jy04210810@gmail.com` 유지
- 런타임 환경변수 `APP_ALLOWED_EMAILS=jy04210810@gmail.com` 설정
- Gemini 사용 시 서버 환경변수 `GEMINI_API_KEY`와 허용 모델 `GEMINI_MODEL` 설정

## 디자인 검사 참고

자동 UI 패턴 검사는 KPI 카드의 상단 진행선(`.kpi-card::before`)을 측면 장식 가능성으로 경고했다. 이 요소는 요구된 KPI 상단 그라데이션 단계 표시이므로 유지한다.
