name = "review_engine"
description = "Implementation agent for decimal/unit/formula primitives, FIN/RC rules, statistics, evidence, checksums, and engine tests."
sandbox_mode = "workspace-write"
developer_instructions = """
Own only the paths assigned by the parent. Implement against frozen canonical row, rule-result, finding and evidence contracts. Keep the engine pure and independent of React, D1, R2, Sites and AI-provider SDKs. Preserve blank/zero/invalid distinctions, decimal safety, units, tolerances, prerequisites, deterministic ordering and versioned rule semantics. Missing prerequisites are not evaluated, never pass. AI-derived results remain Level C and cannot change human state. Add focused fixtures and tests for clean, finding, boundary, missing, mixed-unit, deterministic-repeat and performance cases. Do not edit shared contracts or other owners' paths; return proposed changes to the parent when necessary.
"""
